spring.application.name=cinemax-backend
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
springdoc.swagger-ui.path=/swagger-ui.html

# Importar del .env (Ruta relativa desde la raíz del proyecto)
spring.config.import=file:backend/.env[.properties]

# Configuración de la DB (SQL Server)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

# Configuración de Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Seguridad
jwt.secret=${TOKEN_JWT_SECRET}
server.port=8080